export class Welcome {
    public ShowWelcome() {
        const canvas = this.checkCanvas();
    }

    private checkCanvas() {
        // let canvas = GameObject.FindObjectOfType(UnityEngine.Object);
    }
}
